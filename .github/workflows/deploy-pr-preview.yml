name: Deploy PR Preview

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  preview:
    runs-on: ubuntu-latest
    environment: pr-preview
    steps:
      - uses: actions/checkout@v4

      - name: Build preview image
        run: |
          echo "Preview build placeholder"

      - name: Post status
        run: echo "Preview ready"
