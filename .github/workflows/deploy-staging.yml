name: Deploy Staging

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: staging
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GHCR
        if: secrets.GHCR_PAT
        env:
          GHCR_PAT: ${{ secrets.GHCR_PAT }}
        run: echo "Authenticated"

      - name: Build and push image (no-op placeholder)
        run: echo "Deploy placeholder - configure your staging deploy here"
